Types of java program:-
i]Application-like use standalone machine on LAN.
ii]Applet-is java program are used on internet.
Application means a java program which is used only for a standalone machine on local area network
Applet is a java programm which are used on internet.

*JDK,JRE & JVM:-
JDK-java devlopement kit.which contains different tools which required to devloped & run programm.

JRE-Java runtime enviorement is set of tools,which are used to devloped
java application.

JVM-java virtual machine.is called as virtual machine because it doesnt 
physicaly exist but some external program are there to run the java application.

1]class             2]method 
class class_name     void main()  
{                    {
 void main()         }
{
}
}
*Specifier & Modifier:-
@C lang:=2 Specifier:- private:=variables. 
                       public:=methods.
@CPP:=3 Specifier:-private:=variables. 
                   public:=methods,const.
                   protected:=methods-Inheritance.
@JAVA:=4 Modifier:-
        private:variables,const. 
        public:=methods,constructor.
        protected--Inheritance.
        default:=variables,constructor.

Syntax of class:-
<Access specifier> or/ <modifier>class class name
{
  //variables
<Acess specifier> or/ <modifier>data type variable name
 //methods
<Access specifier><modifier>return type method name(parameters)
{

}
}
Example of class
public class Abc
{
  int roll_no=10; //initialization
  String name="india";

 public void show()--paranthesis
{
char gender='F';
 System.out.println("rollno"+roll_no+"\n Name:-"+name);
}

 public static void main(String args[]) //calling method
{
 Abc a1=new Abc();//memory creation dynamically in java--heap memory--temporary.
 a1.show();
} 
}

*Variables:-Is a quantity which is used to store the values,It's name given
to memory location.
Syntax:-data_type variable_name;
Ex:-int x=10;

*Class:- class is group of object or Collection of element.we can say its a blueprint.or we can say 
its logical entity.ex:-class A

*Object:-Object is runtime entity.or we can say object is a real time entity.Object is physically exist 
inside the memory.//Heap Temporary memory.
ex:  Abc A1=new Abc();



